// C:\DataFlex Projects\FutbolWebApp\AppSrc\SelectLiga.wo
// SelectLiga
//

Use cWebView.pkg
Use cWebList.pkg
Use cWebMenuGroup.pkg
Use cWebMenuItem.pkg
Use cWebColumnButton.pkg
Use cWebColumn.pkg
Use cWebHtmlBox.pkg

Use cligaDataDictionary.dd

Object oSelectLiga is a cWebView
    Set psCaption to "Ligas"
    Set peWebViewStyle to wvsDrilldown
    Set peViewType to vtSelect
    Set piColumnCount to 12
    Set pbShowCaption to False
    Set piMaxWidth to 1024
    Set psCSSClass to "SelectView SelectLigaView"

    Object oliga_DD is a cligaDataDictionary
    End_Object 

    Set Main_DD to oliga_DD
    Set Server  to oliga_DD


    Object oSelectHeader is a cWebHtmlBox
        // Cabecera visual del listado
        Set piColumnSpan to 0
        Set psCSSClass to "ModuleHeader SelectHeader"
        Set psHtml to '<section class="ModuleHeaderCard"><div><span class="ModuleEyebrow">Competicion</span><h1>Ligas</h1><p>Administra competiciones, pais y capacidad maxima de equipos.</p></div><div class="ModuleHeaderBadge">Listado</div></section>'
    End_Object

    // Lista principal de ligas
    Object oList is a cWebList
        Set piColumnSpan to 0
        Set psCSSClass to "MobileList SelectList ModuleList"
        Set pbServerOnRowClick to True
        Set pbFillHeight to True
        Set pbShowHeader to True
        Set piSortColumn to 0

        Object oligaNombre is a cWebColumn
            Entry_Item liga.Nombre
            Set psCaption to "Liga"
            Set piWidth to 500
        End_Object 
        Object oligaPais is a cWebColumn
            Entry_Item liga.Pais
            Set psCaption to "Pais"
            Set piWidth to 300
            Set pbNewLine to False
        End_Object 

        Object oligaEquipos is a cWebColumn
            Entry_Item liga.Equipos
            Set psCaption to "Equipos"
            Set piWidth to 200
            Set pbNewLine to False
        End_Object 

        Object oligaEquiposMax is a cWebColumn
            Entry_Item liga.EquiposMax
            Set psCaption to "Capacidad"
            Set piWidth to 200
            Set pbNewLine to False
        End_Object 

        Object oDetailButton is a cWebColumnButton
            // Boton para abrir la ficha de la liga
            Set psCaption to "Detalle"
            Set piWidth to 45
            Set pbFixedWidth to True
            Set pbResizable to False
            Set piListRowSpan to 4
            Set psBtnCssClass to "WebButtonIcon WebIcon_Info"
            Set peAlign to alignLeft


            WebRegisterPath ntNavigateForward oZoomLiga

            Procedure OnClick
                // Abre el zoom desde el boton de detalle
                Send NavigatePath
            End_Procedure

            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                Move True to NavigateData.bReadOnly
            End_Procedure

        End_Object 

        WebRegisterPath ntNavigateForward oZoomLiga

        Procedure OnRowClick String sRowId
            tWebNavigateData NavigateData
            Get GetNavigateData to NavigateData

            Case Begin
                Case (NavigateData.eNavigateType = nfFromParent)
                    // Si viene desde un padre, abre la ficha de la liga
                    Send NavigateForward of oZoomLiga Self
                    Case Break

                Case (NavigateData.eNavigateType = nfFromChild)
                    // Si vuelve desde un hijo, cierra el selector
                    Send NavigateClose Self
                    Case Break

                Case (NavigateData.eNavigateType = nfFromMain)
                    // Si es una busqueda principal, devuelve el registro elegido
                    Send NavigateClose Self
                    Case Break

                Case Else // nfUndefined
                    // Entrada normal desde el menu o dashboard
                    Send NavigateForward of oZoomLiga Self

            Case End
        End_Procedure

        Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
            Move True to NavigateData.bReadOnly
        End_Procedure

    End_Object 

    Object oActionGroup is a cWebMenuGroup

        Object oSearch is a cWebMenuItem
            Set psCSSClass to "WebPromptMenuItem"
            Set psCaption to "Buscar"

            Procedure OnClick
                // Abre la busqueda de la lista
                Send Search of oList
            End_Procedure

        End_Object 

        Object oNewButton is a cWebMenuItem
            Set psCSSClass to "WebClearMenuItem"
            Set psCaption to "Nueva liga"


            WebRegisterPath ntNavigateForward oZoomLiga

            Procedure OnClick
                // Abre el zoom preparado para una liga nueva
                Send NavigatePath
            End_Procedure

            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                Move True to NavigateData.bNewRecord
            End_Procedure

        End_Object 
    End_Object 

    Procedure OnNavigateForward tWebNavigateData NavigateData Integer hoInvokingView Integer hoInvokingObject
        WebSet pbRender of oNewButton to True
        WebSet pbRender of oDetailButton to True

        Case Begin
            Case (NavigateData.eNavigateType = nfFromParent)
                // Navegacion desde una vista padre
                Case Break

            Case (NavigateData.eNavigateType = nfFromChild)
                // Navegacion al volver desde una vista hija
                Case Break

            Case (NavigateData.eNavigateType = nfFromMain)
                // Cuando se usa como busqueda, no hace falta el boton de detalle
                WebSet pbRender of oDetailButton to False
                Case Break

            Case Else // nfUndefined
                // Entrada normal desde el menu o dashboard

        Case End

    End_Procedure


End_Object 
